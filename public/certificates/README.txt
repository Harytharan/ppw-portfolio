Placeholder certificate
========================

The site links to /certificates/certificate.svg by default.

If your institution requires a PNG:

1. Export your certificate as PNG or PDF.
2. Save it as public/certificates/certificate.png (or update hrefs in components/CertificateEvidence.tsx).
